# Calculates the number of people in a queue

Calculates the number of people in a queue

## Usage

``` r
wl_queue_size_hist(wl_hist)
```

## Arguments

- wl_hist:

  dataframe. A waiting list in histogram format

## Value

integer. The size of the queue or waiting list
